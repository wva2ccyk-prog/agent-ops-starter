name: tmp-checker-parity-repro
on:
  push:
    branches:
      - fix/checker-parser-parity-20260809
  workflow_dispatch:

jobs:
  reproduce:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run current checker repro
        shell: pwsh
        run: ./.github/tmp-checker-parity-repro.ps1
